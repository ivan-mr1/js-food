/**
 * Application configuration
 * BASE_URL — root path for assets (set in vite.config.js)
 * Locally: '' (empty)
 * Production (GitHub Pages): '/Ivan_Fomin/'
 */

export const BASE_URL = import.meta.env.BASE_URL;
